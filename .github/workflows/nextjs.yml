- name: Setup Node
  uses: actions/setup-node@v4
  with:
    node-version: 18
    cache: pnpm

- name: Enable pnpm
  run: corepack enable

- name: Install dependencies
  run: pnpm install

- name: Build
  run: pnpm build
